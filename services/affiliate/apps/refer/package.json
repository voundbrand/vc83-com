{
  "name": "@refref/refer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "infisical run --env=dev -- tsx watch src/index.ts || tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "infisical run --env=dev -- vitest run || vitest run",
    "test:watch": "infisical run --env=dev -- vitest || vitest",
    "test:ui": "infisical run --env=dev -- vitest --ui || vitest --ui",
    "test:run": "infisical run --env=dev -- vitest run || vitest run",
    "type:check": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "fly:deploy": "cd ../../ && flyctl deploy --app refref-refer --dockerfile Dockerfile.refer --config apps/refer/fly.toml --build-arg GIT_SHA=$(git rev-parse --short HEAD) --env GIT_SHA=$(git rev-parse --short HEAD)"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.3.0",
    "@refref/coredb": "workspace:*",
    "@refref/types": "workspace:*",
    "@refref/utils": "workspace:*",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.7",
    "fastify": "^5.2.0",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vitest/ui": "^2.1.8",
    "pino-pretty": "^13.0.0",
    "playwright": "^1.49.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
