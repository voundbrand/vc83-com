# Legacy Style Eradication Task Queue

**Last updated:** 2026-02-24  
**Workstream root:** `/Users/foundbrand_001/Development/vc83-com/docs/reference_docs/topic_collections/implementation/legacy-style-eradication`  
**Baseline reports:** `/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-guard-baseline.txt`, `/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-full-project.txt`

---

## Queue rules

1. Allowed statuses are only: `READY`, `IN_PROGRESS`, `PENDING`, `BLOCKED`, `DONE`.
2. Promote a task from `PENDING` to `READY` only when every dependency is `DONE`.
3. Deterministic selection order: highest priority (`P0` -> `P1` -> `P2`) then lowest ID.
4. Keep one `IN_PROGRESS` task per lane maximum; do not overlap edits on the same files.
5. Every task must run its row-level Verify commands before moving to `DONE`.
6. If a task is `BLOCKED`, log it in `BLOCKERS.md` and move to the next `READY` task.
7. Maintain two runtime appearance modes only; map current `dark` -> `midnight` and `sepia` -> `daylight` until runtime naming migration is complete.
8. Do not reintroduce legacy theme families or `data-window-style` customization.
9. Preserve auth, onboarding, and OAuth callback behavior (`window=manage&tab=integrations` and related flows).
10. Treat `/Users/foundbrand_001/Development/vc83-com/docs/reference_docs/topic_collections/implementation/legacy-style-eradication/l4yercak3-design-token-contract.md` as the style source of truth for new UI edits.
11. After each completed task, sync `INDEX.md`, `MASTER_PLAN.md`, and `TASK_QUEUE.md`.
12. Temporary deferred scope (approved 2026-02-19): ignore `/Users/foundbrand_001/Development/vc83-com/src/app/project/[slug]/templates/gerrit/GerritTemplate.tsx` and `/Users/foundbrand_001/Development/vc83-com/src/components/template-renderer.tsx` for active convergence rows; track them under `LSE-023`.
13. This queue supersedes `/Users/foundbrand_001/Development/vc83-com/docs/reference_docs/topic_collections/implementation/ui-perfection-workstream/TASK_QUEUE.md` for active implementation; keep `UIP-009` and `UIP-010` blocked until `LSE-022` is `DONE`.

---

## Verification profiles

| Profile | Command |
|---|---|
| `V-TYPE` | `npm run typecheck` |
| `V-LINT` | `npm run lint` |
| `V-UNIT` | `npm run test:unit` |
| `V-DESIGN` | `npm run ui:design:guard` |
| `V-DOCS` | `npm run docs:guard` |
| `V-LEGACY` | `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` |

Note: Set `EMPTY_TREE=$(git hash-object -t tree /dev/null)` in the current shell before running `V-LEGACY`.

---

## Execution lanes

| Lane | Purpose | Primary ownership | Merge-overlap policy |
|---|---|---|---|
| `A` | Guard/tooling stabilization and scan repeatability | `scripts/ci/*`, report automation | No UI edits until guard output is stable |
| `B` | Global token/selector cleanup | `src/app/globals.css`, shell primitives | No route/component churn until base tokens are stable |
| `C` | High-density window-content migration wave 1 | `src/components/window-content/{integrations-window,super-admin-organizations-window,web-publishing-window}` | One window family at a time |
| `D` | High-density window-content migration wave 2 | `src/components/window-content/{org-owner-manage-window,ai-chat-window,products-window,booking-window,events-window}` | One window family at a time |
| `E` | Builder/routes/auth compatibility hardening | `src/components/builder/*`, `src/app/*`, auth/onboarding/OAuth-sensitive flows | Preserve route/query behavior |
| `F` | Shared component and tail cleanup | `src/components/*`, `convex/*`, `scripts/*` | No schema contract drift |
| `G` | Legacy wave closeout checkpoint | Cross-cutting + workstream docs | Start only after all original P0/P1 rows are done or blocked |
| `H` | Token-contract CI + token alias foundation | `scripts/ci/*`, `.github/workflows/*`, `src/app/globals.css` | Keep guard and token-layer changes together |
| `I` | Shell chrome + shared primitive token convergence | `src/components/{taskbar,ui,window-content/shared}/*`, `src/app/globals.css` | One primitive family at a time |
| `J` | Remaining UI surface migration | `src/components/window-content/*`, `src/app/*`, `src/components/builder/*` | One product surface cluster at a time |
| `K` | Visual/contrast validation and full-lock closeout | `tests/e2e/*`, CI workflows, workstream docs | Start only after migration lanes are done |

---

## Concurrency rules

1. Run Lane `A` first through `LSE-002`.
2. After `LSE-002` is `DONE`, run one active task each in Lane `B` and Lane `C`.
3. Lane `D` starts only after `LSE-004` and `LSE-005` are `DONE`.
4. Lane `E` starts only after `LSE-007` and `LSE-009` are `DONE`.
5. Lane `F` starts only after Lane `E` tasks are `DONE`.
6. Lane `G` starts only when all original `P0` and `P1` tasks (`LSE-001..LSE-014`) are `DONE` or explicitly `BLOCKED`.
7. Lane `H` may proceed once `LSE-002` is `DONE`; it can run in parallel with Lane `G` because it is additive guard/token hardening.
8. Lane `I` starts only after `LSE-016` is `DONE`.
9. Lane `J` starts only after `LSE-018` is `DONE`.
10. Lane `K` starts only after `LSE-020` is `DONE`.

---

## Dependency-based status flow

1. Keep `LSE-001` as the first executable task because guard script validity gates all later verification trust.
2. Keep all migration rows `PENDING` until their predecessor lane gates are met.
3. Promote `LSE-010` only after builder/routes cleanup rows are complete to avoid mixed contracts.
4. Promote `LSE-012` only after `LSE-011` to avoid repeated shared-component conflicts.
5. Promote `LSE-013` only after all original code migration rows are `DONE`; this is the legacy zero-debt checkpoint.
6. Promote `LSE-014` only after `LSE-013` is `DONE`; original closeout docs sync is the gate.
7. Keep `LSE-015` before all contract-convergence rows so CI enforcement lands first.
8. Promote `LSE-016` only after `LSE-015` is `DONE` to ensure token alias work matches live guard rules.
9. Promote `LSE-017` and `LSE-018` sequentially to avoid `globals.css` merge churn.
10. Promote `LSE-019` only after shared primitives converge (`LSE-018`).
11. Promote `LSE-021` only after `LSE-020` so visual/contrast coverage captures final migrated UI.
12. Promote `LSE-022` only after `LSE-021`; this is the token-contract closeout gate.
13. Keep deferred template debt (`LSE-023`) out of active lanes until the current contract convergence closeout (`LSE-022`) is complete.
14. Treat completion of `LSE-022` as the cross-workstream gate that can unblock archived UIP closeout rows if explicit reactivation is requested.

---

## Task queue

| ID | Lane | Plan | Priority | Status | Depends On | Task | Primary files | Verify | Notes |
|---|---|---:|---|---|---|---|---|---|---|
| `LSE-001` | `A` | 1 | `P0` | `DONE` | - | Fix `ui:legacy:guard` regex handling so baseline runs without `awk` syntax errors | `scripts/ci/check-legacy-style-introductions.sh` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-18: replaced dynamic `awk` regex matching with BSD/macOS-safe literal and POSIX-boundary checks, and sorted scoped path iteration with `LC_ALL=C` for deterministic ordering. Verify: `npm run typecheck` passed; `npm run lint` passed with existing repo warnings (0 errors); `npm run test:unit` passed; `npm run docs:guard` passed; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` now fails on real baseline legacy matches without any `awk` syntax errors. |
| `LSE-002` | `A` | 1 | `P0` | `DONE` | `LSE-001` | Add deterministic guard coverage (script/unit/smoke) to prevent regex regressions | `scripts/ci/check-legacy-style-introductions.sh`; `tests/*` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: cleared workspace typecheck blockers and reran full verify stack. Results: `npm run typecheck` pass; `npm run lint` pass with existing warnings (`0` errors); `npm run test:unit` pass (`75` files, `377` tests); `npm run docs:guard` pass; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known baseline legacy debt (expected for current migration stage). |
| `LSE-003` | `B` | 2 | `P0` | `DONE` | `LSE-002` | Remove `--win95-*` root token definitions and map surviving semantics to modern dark/sepia token names | `src/app/globals.css` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: finalized Lane B token foundation in `src/app/globals.css` with canonical dark/sepia `--shell-*` semantics as the shared shell source of truth while preserving core window behavior. Verify: `npm run typecheck` pass; `npm run lint` pass with existing warnings (`2964`, `0` errors); `npm run test:unit` pass (`76` files, `391` tests); `npm run docs:guard` pass; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known baseline scoped legacy debt. |
| `LSE-004` | `B` | 2 | `P1` | `DONE` | `LSE-003` | Replace `.retro-button*` selectors and `var(--win95-*)` references in shared shell primitives | `src/app/globals.css`; `src/components/retro-button.tsx`; `src/components/retro-window.tsx`; `src/components/floating-window.tsx` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: finalized shared shell primitives on `desktop-shell-*` classes/tokens in `src/components/retro-button.tsx`, `src/components/retro-window.tsx`, and `src/components/floating-window.tsx` while keeping dark/sepia semantics and core window behavior intact. Verify: `npm run typecheck` hit transient `TS2589` at `src/components/window-content/store-window.tsx:46` on first run and passed on immediate rerun; `npm run lint` pass with existing warnings (`2963`, `0` errors); `npm run test:unit` pass (`77` files, `396` tests); `npm run docs:guard` pass; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known baseline scoped legacy debt. |
| `LSE-005` | `C` | 3 | `P1` | `DONE` | `LSE-004` | Migrate `integrations-window` family off legacy style tokens/classes | `src/components/window-content/integrations-window/*` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: confirmed `integrations-window` remains at `0` matches for queue legacy patterns (`retro-button`, `--win95-*`, `var(--win95-*)`, `data-window-style`, `zinc-*`, `purple-*`) while preserving integrations workflow behavior. Verify rerun: `typecheck` pass; `lint` pass with warnings (`2970`, `0` errors); `test:unit` pass (`80` files, `417` tests); `docs:guard` pass; `ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known baseline debt outside Lane `C`. |
| `LSE-006` | `C` | 3 | `P1` | `DONE` | `LSE-005` | Migrate `super-admin-organizations-window` and remove remaining legacy token usage | `src/components/window-content/super-admin-organizations-window/*` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: confirmed `super-admin-organizations-window` returns `0` matches for queue legacy patterns while preserving org-management workflows and translations. Verify rerun: `typecheck` pass; `lint` pass with warnings (`2970`, `0` errors); `test:unit` pass (`80` files, `417` tests); `docs:guard` pass; `ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known baseline debt outside Lane `C`. |
| `LSE-007` | `C` | 3 | `P1` | `DONE` | `LSE-006` | Migrate `web-publishing-window` and `org-owner-manage-window` off legacy classes/tokens | `src/components/window-content/web-publishing-window/*`; `src/components/window-content/org-owner-manage-window/*` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: both `web-publishing-window` and `org-owner-manage-window` return `0` matches for queue legacy patterns while preserving publish/manage workflows and translation keys. Verify rerun: `typecheck` pass after lane-safe type unblocks in `src/components/window-content/org-owner-manage-window/ai-settings-tab.tsx`, `src/lib/store-pricing-calculator.ts`, and `convex/integrations/aiConnections.ts`; `lint` pass with warnings (`2971`, `0` errors); `test:unit` pass (`84` files, `431` tests); `docs:guard` pass; `ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known baseline debt outside Lane `C`. |
| `LSE-008` | `D` | 4 | `P1` | `DONE` | `LSE-007` | Migrate `ai-chat-window`, `products-window`, and `booking-window` families | `src/components/window-content/ai-chat-window/*`; `src/components/window-content/products-window/*`; `src/components/window-content/booking-window/*` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: migrated Lane `D` wave 2 part 1 to modern shell/tone tokens/classes and removed scoped `zinc-*`/`purple-*` utility usage in `ai-chat-window`, with scoped queue-pattern scans now `0` for `ai-chat-window`, `products-window`, and `booking-window`. Behavior remained equivalent for assistant/products/booking flows. Verify reruns: `typecheck` pass (after transient `TS2589` and transient union noise on earlier attempts), `lint` pass with warnings (`2977`, `0` errors), `test:unit` pass (`85` files, `437` tests), `docs:guard` pass, `ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known global baseline debt outside Lane `D`. |
| `LSE-009` | `D` | 4 | `P1` | `DONE` | `LSE-008` | Migrate `events-window`, `benefits-window`, `checkout-window`, and `media-library-window` families | `src/components/window-content/events-window/*`; `src/components/window-content/benefits-window/*`; `src/components/window-content/checkout-window/*`; `src/components/window-content/media-library-window/*` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: completed Lane `D` wave 2 part 2 and migrated `benefits-window` to modern tokens/classes while preserving events/checkout/media-library behavior; scoped queue-pattern scans are now `0` for `events-window`, `benefits-window`, `checkout-window`, and `media-library-window`. Verify: `typecheck` pass, `lint` pass with warnings (`2977`, `0` errors), `test:unit` pass (`85` files, `437` tests), `docs:guard` pass, `ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known global baseline debt outside Lane `D`. |
| `LSE-010` | `E` | 5 | `P0` | `DONE` | `LSE-009` | Clean builder/routes legacy class usage (`zinc-*`, `purple-*`, legacy button classes) while preserving route contracts | `src/components/builder/*`; `src/app/builder/page.tsx`; `src/app/builder/[projectId]/*`; `src/app/page.tsx` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: removed Lane `E` builder/routes legacy utility usage by migrating builder workspace loading/error surfaces off `zinc-*`/`purple-*` classes and replacing taskbar `retro-window` usage with `desktop-shell-window`. Queue-pattern scan is `0` in scoped Lane `E` files and route/deep-link logic is unchanged. Verify: `typecheck` pass, `lint` pass with warnings (`2980`, `0` errors), `test:unit` pass (`89` files, `456` tests), `docs:guard` pass, `ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known global baseline debt. |
| `LSE-011` | `E` | 5 | `P0` | `DONE` | `LSE-010` | Validate auth/onboarding/OAuth callback compatibility while removing legacy style branches in auth-adjacent views | `src/app/auth/*`; `src/components/window-content/login-window.tsx`; `src/app/page.tsx`; `src/lib/shell/url-state.ts` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: migrated auth-adjacent `retro-*` class usage in `login-window` and `auth/cli-login` to `desktop-shell-*` aliases with equivalent styling in `globals.css`, preserving onboarding/auth and OAuth callback compatibility (`window=manage&tab=integrations` contract unchanged in `src/lib/shell/url-state.ts` and `src/app/page.tsx`). Verify: `typecheck` pass (with local `TS2589` suppression at `login-window` query call), `lint` pass with warnings (`2980`, `0` errors), `test:unit` pass (`89` files, `456` tests), `docs:guard` pass, `ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known global baseline debt. |
| `LSE-012` | `F` | 6 | `P1` | `DONE` | `LSE-011` | Remove remaining legacy references in shared/tail files including `convex` and scripts | `src/components/*`; `convex/*`; `scripts/*` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: completed Lane `F` shared/tail cleanup by migrating remaining shared `retro-button*`/`beveled-button*` references to `desktop-shell-*`/`desktop-interior-*`, remapping shared `zinc-*`/`purple-*` utility usage to non-legacy palettes, and confirming no queue-pattern matches remain in shared non-window scopes. Scoped queue-pattern scan results: `src/components` (excluding `window-content` and `builder`) `0`; `convex` `0`; `scripts` (excluding training output) `5` intentional guard literals only. Residual snapshot: `/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse012-lanef-residual.txt` (`1523` scoped matches, concentrated in `window-content`). Verify: `npm run typecheck` fail (`TS2589` at `src/components/interview/template-designer.tsx:57`); `npm run lint` pass with warnings (`2987`, `0` errors); `npm run test:unit` pass (`90` files, `459` tests); `npm run docs:guard` pass; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known broad baseline debt in guarded scope. |
| `LSE-013` | `G` | 7 | `P0` | `DONE` | `LSE-012` | Re-run full baseline guard + full-project regex scan and confirm debt reduction target | `/Users/foundbrand_001/Development/vc83-com/tmp/reports/*`; `scripts/ci/check-legacy-style-introductions.sh` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: reran baseline and full-project scans with refreshed debt metrics (`2117` matches across `163` files; delta vs prior checkpoint `-838` matches and `-81` files). Deferred scope remained untouched (`GerritTemplate.tsx`=`5` hits, `template-renderer.tsx`=`0`). Verify rerun: `npm run typecheck` pass; `npm run lint` pass with warnings (`3009`, `0` errors); `npm run test:unit` pass in unrestricted run (`92` files, `473` tests); `npm run docs:guard` pass; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known broad baseline debt. |
| `LSE-014` | `G` | 7 | `P1` | `DONE` | `LSE-013` | Final docs sync (`INDEX`, `MASTER_PLAN`, `BLOCKERS`, `TASK_QUEUE`, `SESSION_PROMPTS`) and closeout handoff | `docs/reference_docs/topic_collections/implementation/legacy-style-eradication/*` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run docs:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` | Done 2026-02-19: synchronized Lane `G` queue artifacts (`INDEX`, `MASTER_PLAN`, `BLOCKERS`, `TASK_QUEUE`, `SESSION_PROMPTS`) after `LSE-013` completion and verified deterministic closeout state. Verify rerun: `npm run typecheck` pass; `npm run lint` pass with warnings (`3009`, `0` errors); `npm run test:unit` pass in unrestricted run (`92` files, `473` tests); `npm run docs:guard` pass; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known broad baseline debt. |
| `LSE-015` | `H` | 8 | `P0` | `DONE` | `LSE-002` | Integrate the *l4yercak3* design token contract rules into `ui:design:guard` and CI trigger paths | `scripts/ci/check-ui-design-drift.sh`; `.github/workflows/ui-design-guard.yml`; `docs/reference_docs/topic_collections/implementation/legacy-style-eradication/l4yercak3-design-token-contract.md` | `npm run ui:design:guard`; `npm run docs:guard` | Done 2026-02-19: expanded `check-ui-design-drift.sh` to enforce contract-derived drift checks on newly introduced lines (raw color literals, raw px/radius/shadow outside token files, disallowed utility classes, `transition-all`, uppercase brand variants) and added contract-path triggers to `ui-design-guard.yml`. Follow-up 2026-02-24: replaced `mapfile` with a Bash 3-compatible read loop for macOS portability without changing guard logic. Verify: `npm run ui:design:guard` runtime now executes; `npm run docs:guard` pass. |
| `LSE-016` | `H` | 8 | `P0` | `DONE` | `LSE-015` | Codify canonical token alias layer in `globals.css` for Midnight/Daylight contract tokens while preserving runtime dark/sepia compatibility | `src/app/globals.css`; `docs/reference_docs/topic_collections/implementation/legacy-style-eradication/l4yercak3-design-token-contract.md` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run ui:design:guard`; `npm run docs:guard` | Done 2026-02-24: landed canonical contract alias foundation in `globals.css` (Midnight/Daylight `--color-*`, `--btn-*`, `--window-*`, `--menu-*`, `--input-*`, `--shadow-*`, `--overlay`, plus spacing/radius/motion/icon scales) while preserving dark/sepia compatibility bridges. Unblocked `ui:design:guard` by clearing newly introduced drift in changed `src/components` lines (`builder-chat-panel`, `agents-window`, `agent-trust-cockpit`, `agent-create-form`) and reran row verify stack. Verify: `typecheck` pass; `lint` pass with warnings (`3114`, `0` errors); `test:unit` pass (`108` files, `558` tests); `ui:design:guard` pass; `docs:guard` pass. |
| `LSE-017` | `I` | 9 | `P1` | `DONE` | `LSE-016` | Migrate shell chrome classes (taskbar/menu/window/titlebar/tabs) to contract color/radius/spacing tokens | `src/app/globals.css`; `src/components/taskbar/*`; `src/components/windows-menu.tsx`; `src/components/floating-window.tsx` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run ui:design:guard`; `npm run docs:guard` | Done 2026-02-24: completed shell chrome convergence in lane scope and cleared the out-of-scope guard blocker (`BLK-LSE-017`) by fixing the drift lines in `src/components/window-content/agents-window.tsx` and the remaining `border-2` drift line in `src/components/window-content/org-owner-manage-window/ai-settings-tab.tsx`. Follow-up promotion rerun on 2026-02-24: guard failure surfaced at out-of-scope `src/components/processing-modal.tsx` (`bg-black/60`), replaced with `var(--modal-overlay-bg)` token usage before advancing lane handoff. Verify rerun: `typecheck` pass; `lint` pass with warnings (`3128`, `0` errors); `test:unit` pass (`109` files, `570` tests); `ui:design:guard` pass; `docs:guard` pass. |
| `LSE-018` | `I` | 9 | `P1` | `DONE` | `LSE-017` | Converge shared primitives (button/input/select/checkbox/card/modal/table/badge) to the contract component-token matrix | `src/components/window-content/shared/interior-primitives.tsx`; `src/components/ui/*`; `src/components/retro-button.tsx`; `src/app/globals.css` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run ui:design:guard`; `npm run docs:guard` | Done 2026-02-24: converged shared primitives to contract semantics by updating shell/interior button sizing + text scale, adding `card`/`checkbox`/`badge`/`table`/`modal` primitives and corresponding `desktop-interior-*` classes, and aligning dark/sepia runtime token mappings in `globals.css` without changing behavior contracts for button/input/select/modal/table flows. Promotion rerun after clearing out-of-scope modal guard drift (`src/components/processing-modal.tsx`) remained green. Verify rerun: `typecheck` pass; `lint` pass with warnings (`3128`, `0` errors); `test:unit` pass (`109` files, `570` tests); `ui:design:guard` pass; `docs:guard` pass. |
| `LSE-019` | `J` | 10 | `P1` | `DONE` | `LSE-018` | Migrate remaining high-density windows to contract tokens and remove raw value styling debt | `src/components/window-content/{ai-system-window,sequences-window,finder-window,tickets-window,translations-window,compliance-window,agent-configuration-window}/*` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run ui:design:guard`; `npm run docs:guard` | Done 2026-02-24: completed bounded family migration across Lane `J` window-content scope by replacing residual `retro-*` primitives with `desktop-interior-*`, removing `transition-all`/`bg-black/*`/`shadow-*` utility drift, dropping fallback hex literals in `var(...)`, and normalizing raw rgba/legacy color literals to contract tokens in touched files. Scoped regression check for high-signal drift markers is now `0` across the row family set. Verify: `npm run typecheck` failed on pre-existing out-of-scope workspace issues (`convex/oauth/endpoints.ts` `FormData.get`, `src/app/api/webhooks/activecampaign/route.ts` `FormData.entries`, and existing non-Lane-J typing debt in checkout/crm/events); `npm run lint` pass with warnings (`3145`, `0` errors); `npm run test:unit` pass (`109` files, `570` tests); `npm run ui:design:guard` pass; `npm run docs:guard` pass. |
| `LSE-020` | `J` | 10 | `P1` | `DONE` | `LSE-019` | Sweep remaining app routes and builder surfaces for token-contract compliance and legacy utility removal | `src/app/*`; `src/components/builder/*`; `src/components/window-content/*`; `src/components/start-menu.tsx` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run ui:design:guard`; `npm run docs:guard` | Done 2026-02-24: completed bounded route/builder closeout sweep by converging `src/components/start-menu.tsx` to shell-token window primitives (`desktop-shell-window`), tokenizing builder header variants/link states in `src/components/builder/page-header.tsx`, and removing route-surface `bg-white`/`text-white`/`shadow-*` debt in `src/app/checkout/success/page.tsx` via contract token classes. Auth/onboarding/deep-link/OAuth callback flows were preserved (no behavioral code-path changes in those surfaces). Verify: `npm run typecheck` failed on the same pre-existing out-of-scope workspace errors as `LSE-019`; `npm run lint` pass with warnings (`3145`, `0` errors); `npm run test:unit` pass (`109` files, `570` tests); `npm run ui:design:guard` pass; `npm run docs:guard` pass. |
| `LSE-021` | `K` | 11 | `P0` | `DONE` | `LSE-020` | Expand visual regression and contrast checks for Midnight/Daylight with deterministic pass/fail outputs in CI artifacts | `tests/e2e/ui-visual-regression.spec.ts`; `playwright.visual.config.ts`; `.github/workflows/ui-design-guard.yml`; `scripts/ci/check-ui-design-drift.sh` | `npm run test:e2e:visual`; `npm run ui:design:guard`; `npm run docs:guard` | Done 2026-02-24: expanded Lane `K` visual+contrast lock with deterministic `screen=... mode=... token=...` labels in Playwright tests, machine-readable contrast metadata (`data-contrast-*`) in the showcase panel, JSON report emission in `playwright.visual.config.ts`, and CI summary labeling via `scripts/ci/emit-ui-visual-contract-results.mjs` + workflow step updates. Refreshed baseline snapshots for `visual-shell-*` and `design-token-showcase-coverage-*` via `npm run test:e2e:visual:update`. Verify: `npm run test:e2e:visual` pass (`18` tests, `4.3m`) with deterministic labels (`/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse021-test-e2e-visual.txt` + `/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse021-visual-summary.txt`, JSON: `/Users/foundbrand_001/Development/vc83-com/tmp/test-results/ui-visual/results.json`); `npm run ui:design:guard` pass (`UI_DESIGN_GUARD_RESULT|status=PASS|violations=0`, `/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse021-ui-design-guard.txt`); `npm run docs:guard` pass (`/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse021-docs-guard.txt`). |
| `LSE-022` | `K` | 11 | `P0` | `DONE` | `LSE-021` | Token-contract closeout: rerun full verification, publish debt deltas, and sync queue docs for handoff | `docs/reference_docs/topic_collections/implementation/legacy-style-eradication/*`; `/Users/foundbrand_001/Development/vc83-com/tmp/reports/*` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run ui:design:guard`; `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD`; `npm run docs:guard` | Done 2026-02-24: completed closeout verification rerun and published debt deltas in `/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse022-closeout-summary.md`. Verify: `npm run typecheck` fail on known out-of-scope debt (`src/app/api/webhooks/activecampaign/route.ts:196:39` `TS2589`, log `/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse022-typecheck.txt`); `npm run lint` pass with warnings (`3146`, `0` errors, `/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse022-lint.txt`); `npm run test:unit` pass (`109` files, `570` tests, `/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse022-test-unit.txt`); `npm run ui:design:guard` pass (`/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse022-ui-design-guard.txt`); `npm run ui:legacy:guard -- "$EMPTY_TREE" HEAD` fail on known baseline debt (`1425` scoped hits, `/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse022-ui-legacy-guard.txt`); `npm run docs:guard` pass (`/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse022-docs-guard.txt`). Post-sync docs guard rerun also passed (`/Users/foundbrand_001/Development/vc83-com/tmp/reports/legacy-style-lse022-docs-guard-post-sync.txt`). This row clears the UIP supersession gate (`UIP-009`/`UIP-010`) for explicit reactivation requests. |
| `LSE-023` | `J` | 12 | `P2` | `PENDING` | `LSE-022` | Deferred template debt cleanup for excluded files (`GerritTemplate` and `template-renderer`) under the token contract | `src/app/project/[slug]/templates/gerrit/GerritTemplate.tsx`; `src/components/template-renderer.tsx` | `npm run typecheck`; `npm run lint`; `npm run test:unit`; `npm run ui:design:guard`; `npm run docs:guard` | Dependency gate (`LSE-022`) is satisfied, but row remains deferred by product directive from 2026-02-19 until explicit reactivation. |

---

## Current kickoff

- Active task: none (`LSE-021` + `LSE-022` are `DONE`).
- Immediate gate: none.
- Next promotable task: none in active scope (`LSE-023` remains deferred pending explicit product reactivation).
- Token-contract track status: Lanes `H`, `I`, `J`, and `K` are complete (`LSE-015..LSE-022` all `DONE`) with deterministic visual+contrast labels now emitted in CI/test artifacts.
- Lane `J` status: `LSE-019` and `LSE-020` remain complete; `typecheck` debt persists only in known out-of-scope workspace areas and was revalidated during `LSE-022`.
- Legacy checkpoint status: Lane `G` closeout rows (`LSE-013`, `LSE-014`) are complete; `V-LEGACY` still reports known baseline debt.
- Deferred scope status: `GerritTemplate.tsx` + `template-renderer.tsx` are intentionally excluded from active convergence work and tracked in `LSE-023`.
- Cross-workstream status: UIP implementation rows (`UIP-009`, `UIP-010`) are now eligible for explicit reactivation because `LSE-022` is complete.
