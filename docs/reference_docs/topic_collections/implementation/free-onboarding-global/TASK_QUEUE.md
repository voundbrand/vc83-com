# Free Onboarding Global V2 Task Queue

**Last updated:** 2026-02-27  
**Workstream root:** `/Users/foundbrand_001/Development/vc83-com/docs/reference_docs/topic_collections/implementation/free-onboarding-global`  
**Source request:** Document 07 (2026-02-24) free onboarding rewrite: chat-first experience, beta-code auto-approve path, cross-channel onboarding consistency, and business-mode-first agent birth.

---

## Queue rules

1. Allowed statuses are only: `READY`, `IN_PROGRESS`, `PENDING`, `BLOCKED`, `DONE`.
2. Only one task may be `IN_PROGRESS` globally unless lane policy explicitly allows overlap.
3. Promote a task from `PENDING` to `READY` only when all dependencies are `DONE`.
4. Deterministic pick order is `P0` before `P1`, then lowest task ID.
5. If blocked, capture the blocker in `Notes`, set status `BLOCKED`, and continue with next `READY` row.
6. Every task must run listed `Verify` commands before moving to `DONE`.
7. Do not regress existing signed-in chat behavior while introducing guest/onboarding paths.
8. Business mode (`work`) remains default for onboarding unless a row explicitly changes that contract.
9. Sync `INDEX.md`, `MASTER_PLAN.md`, `TASK_QUEUE.md`, and `SESSION_PROMPTS.md` at lane milestones.
10. Onboarding birthing defaults must follow clone-first catalog activation; do not expose operator-default free-form agent creation in onboarding-v2 paths.
11. No-fit onboarding outcomes must escalate to purchase-only custom-agent concierge (no free-form self-build), and any surfaced terms must remain exact: `€5,000 minimum`, `€2,500 deposit`, `includes 90-minute onboarding with engineer`.

---

## Verification profiles

| Profile | Command |
|---|---|
| `V-TYPE` | `npm run typecheck` |
| `V-LINT` | `npm run lint` |
| `V-UNIT` | `npm run test:unit` |
| `V-DOCS` | `npm run docs:guard` |
| `V-ONBOARDING-LINT` | `npx eslint convex/onboarding.ts convex/http.ts convex/betaAccess.ts convex/api/v1/oauthSignup.ts src/components/window-content/login-window.tsx src/components/window-content/ai-chat-window/index.tsx src/app/page.tsx` |

---

## Execution lanes

| Lane | Purpose | Primary ownership | Merge-overlap policy |
|---|---|---|---|
| `A` | Beta code platform foundation (schema, CRUD, validation, signup email auto-approve) | `convex/schemas/*`; `convex/betaCodes.ts`; `convex/http.ts`; `convex/onboarding.ts` | Lane `B` waits for lane `A` `P0` rows |
| `B` | OAuth + identity continuity for beta code redemption/approval | `src/app/api/auth/oauth-signup/route.ts`; `convex/api/v1/oauthSignup.ts`; `convex/schemas/coreSchemas.ts` | No lane `C` onboarding UX handoff before lane `B` `P0` |
| `C` | AI-chat-first onboarding flow in web/native guest surfaces | `src/components/window-content/ai-chat-window/*`; `src/app/page.tsx`; onboarding tools | No messaging-channel rewrites in lane `C` |
| `D` | Messaging channel onboarding parity (WhatsApp/Telegram/Slack/SMS bootstrap) | `convex/channels/*`; `convex/onboarding/*`; channel webhooks | No dashboard/nurture work in lane `D` |
| `E` | Nurture arc + Soul report + specialist preview orchestration | workflows, schedulers, report generators | Starts after lane `C` and lane `D` `P0` |
| `F` | Rollout, kill switches, verification, docs closeout | cross-cutting tests + docs | Starts after all prior `P0` are `DONE` or `BLOCKED` |
| `G` | Catalog-clone birthing convergence | onboarding-v2 birthing handoff + agent activation UX/contracts | Must preserve `FOG2-013` rollout controls and consume AGP clone-first + purchase-only concierge contracts |

---

## Dependency-based status flow

1. Execute lane `A` first (`FOG2-001`..`FOG2-003`).
2. Lane `B` starts after lane `A` `P0` rows are `DONE`.
3. Lane `C` starts after `FOG2-002` and `FOG2-004` are `DONE`.
4. Lane `D` starts after `FOG2-002` is `DONE`.
5. Lane `E` starts after `FOG2-007` and `FOG2-009` are `DONE`.
6. Lane `F` starts after all `P0` tasks are `DONE` or explicitly `BLOCKED`.
7. Lane `G` starts after `FOG2-014` is `DONE` and `AGP-023` is at least `READY`.

---

## Task queue

| ID | Lane | Plan | Priority | Status | Depends On | Task | Primary files | Verify | Notes |
|---|---|---:|---|---|---|---|---|---|---|
| `FOG2-001` | `A` | 1 | `P0` | `DONE` | - | Add beta code data model + backend CRUD + public validation endpoint contract | `convex/schemas/betaOnboardingSchemas.ts`; `convex/schema.ts`; `convex/betaCodes.ts`; `convex/http.ts` | `V-TYPE`; `V-ONBOARDING-LINT`; `V-DOCS` | Done 2026-02-25: added `betaActivationCodes` + `betaCodeRedemptions` schema tables, created `convex/betaCodes.ts` (admin create/list/update/deactivate/batch/export + internal validate/redeem), and added `POST /api/beta/validate` (+ OPTIONS). Verify: targeted eslint warnings-only; docs guard passed; full repo typecheck could not be reliably completed in this session due long-running/stale background `tsc --noEmit` processes. |
| `FOG2-002` | `A` | 1 | `P0` | `DONE` | `FOG2-001` | Integrate email signup beta code path: optional code input, pre-validation, redemption, auto-approve bypass of manual gate | `convex/onboarding.ts`; `convex/http.ts`; `src/components/window-content/login-window.tsx` | `V-TYPE`; `V-ONBOARDING-LINT` | Done 2026-02-25: `/api/signup` now accepts `betaCode` aliases, signup action/mutation validate + redeem beta code and auto-approve when valid (manual pending path preserved for no-code when gate is enabled), and signup UI now exposes optional beta code field with status-aware success messaging. Verify: targeted eslint warnings-only. |
| `FOG2-003` | `A` | 1 | `P1` | `DONE` | `FOG2-001` | Add Super Admin beta code operations surface (filters, deactivate/edit, batch + CSV export) | `src/components/window-content/super-admin-organizations-window/*`; `convex/betaCodes.ts` | `V-TYPE`; `V-LINT` | Done 2026-02-27: added `BetaCodeOperationsPanel` under existing Super Admin `Beta Access` tab while preserving current beta-gating + request-review behavior, shipped beta-code filters (status, channelTag, sourceDetail, codeSearch), inline edit/deactivate controls, batch create surface, and CSV export download flow. Extended `convex/betaCodes.ts` `listBetaCodes` + `exportBetaCodesCsv` query contracts with server-side `sourceDetail` + `codeSearch` filters for list/export parity. Preserved `FOG2-013` rollout/kill-switch/rollback semantics unchanged (policy-only rollback; no Telegram/WhatsApp/Slack/SMS handler rewrites). Verify run exactly in order: `npm run typecheck` passed; `npm run lint` passed with `3377 problems (0 errors, 3377 warnings)`. |
| `FOG2-004` | `B` | 2 | `P0` | `DONE` | `FOG2-002` | Extend OAuth signup state + callback to accept/redeem beta code and auto-approve newly created OAuth accounts | `src/app/api/auth/oauth-signup/route.ts`; `convex/api/v1/oauthSignup.ts`; `convex/schemas/coreSchemas.ts` | `V-TYPE`; `V-ONBOARDING-LINT`; `V-UNIT` | Done 2026-02-25: OAuth signup initiation now accepts/persists `betaCode`, callback completion redeems valid codes for newly created OAuth users, and new OAuth-user beta access status now follows gating+code parity (valid code auto-approves, no-code pending path preserved). Returning OAuth users are still routed through existing-user path without beta-code gating. Also wired signup-mode OAuth launch to forward optional beta code and org name from login UI. Verify: initial run captured transient `npm run typecheck` behavior with no diagnostics; re-verified 2026-02-25 with `npm run typecheck` passing, targeted onboarding eslint warnings-only, and `npm run test:unit` passing (123 files, 587 tests; 4 files/80 tests skipped). |
| `FOG2-005` | `B` | 2 | `P1` | `DONE` | `FOG2-004` | Add beta code redemption analytics (channel/source/device) and admin reporting aggregates | `convex/betaCodes.ts`; `convex/onboarding/*`; analytics emitters | `V-TYPE`; `V-LINT`; `V-UNIT` | Done 2026-02-25: beta redemptions now capture `source` + device/channel attribution, OAuth/email signup flows pass source/device metadata, idempotent redemption path backfills missing analytics fields, and admin aggregate query `getBetaCodeRedemptionAnalytics` now returns window/filter rollups by channel/source/device/code. Added idempotent redemption funnel activation emit keyed by user+code. Verify: re-verified 2026-02-25 with `npm run typecheck` passing, `npm run lint` warnings-only, and `npm run test:unit` passing (123 files, 587 tests; 4 files/80 tests skipped). |
| `FOG2-006` | `C` | 3 | `P0` | `DONE` | - | Force logged-out refresh default to AI chat while preserving explicit login deep links | `src/app/page.tsx`; `src/components/window-content/ai-chat-window/index.tsx` | `V-TYPE`; `V-ONBOARDING-LINT` | Done 2026-02-25: logged-out initial window now opens AI chat by default; explicit `openLogin` and `app=login` still honored. |
| `FOG2-007` | `C` | 3 | `P0` | `DONE` | `FOG2-002`, `FOG2-004`, `FOG2-006` | Implement chat-native onboarding handoff in guest AI chat (beta code prompt, OAuth/email initiation, continuity token) | `src/components/window-content/ai-chat-window/*`; onboarding tools/runtime | `V-TYPE`; `V-ONBOARDING-LINT`; `V-UNIT` | Done 2026-02-25: guest AI chat now shows an in-chat optional beta-code prompt with direct OAuth and email signup initiation CTAs (no forced form-first detour), signup OAuth links now carry optional beta code in guest handoff, and email signup now accepts/consumes `identityClaimToken` to preserve guest continuity. Also extended `start_account_creation_handoff` tool/runtime to accept optional `betaCode` so assistant-generated CTAs can preserve activation context. Verify: `npm run typecheck` passed; targeted onboarding eslint completed warnings-only; `npm run test:unit` passed (124 files, 591 tests; 4 files/80 tests skipped). |
| `FOG2-008` | `C` | 3 | `P1` | `DONE` | `FOG2-007` | Implement business-context birthing question flow + compilation reveal contract | `convex/onboarding/seedPlatformAgents.ts`; `convex/ai/interviewRunner.ts`; `src/components/window-content/ai-chat-window/index.tsx`; onboarding unit tests | `V-TYPE`; `V-LINT`; `V-UNIT` | Done 2026-02-25: updated onboarding template prompts to keep business context first and private/trust context as teaser-level later, added guided-runtime prompt contract for business-context birthing + confirmation compilation reveal, and updated guest AI chat welcome copy to reinforce chat-native business-first setup. Added/updated unit coverage for handshake compilation reveal and onboarding phase-order guardrails. Verify: `npm run typecheck` passed; `npm run lint` completed warnings-only (3253 warnings, 0 errors); `npm run test:unit` passed (125 files, 597 tests; 4 files/80 tests skipped). |
| `FOG2-009` | `D` | 4 | `P0` | `DONE` | `FOG2-002` | Add first-message beta code detection/validation bootstrap for Telegram/WhatsApp/Slack/SMS | `convex/channels/*`; `convex/onboarding/*`; webhook handlers | `V-TYPE`; `V-LINT`; `V-UNIT` | Done 2026-02-25: added channel onboarding bootstrap runtime (`convex/onboarding/channelBootstrap.ts`) for first-message beta code extraction/validation + audit emission, integrated bootstrap execution into shared inbound orchestration (`convex/ai/agentExecution.ts`) with non-blocking metadata enrichment, and added onboarding bootstrap unit coverage. Verify: initial lane-D run completed `npm run typecheck` (pass), `npm run lint` (warnings-only), `npm run test:unit` (pass). |
| `FOG2-010` | `D` | 4 | `P1` | `DONE` | `FOG2-009` | Implement post-birth channel connection prompts and first-message SLA tracking | channel integration + telemetry | `V-TYPE`; `V-LINT`; `V-UNIT` | Done 2026-02-25: shipped connected-channel first-message latency tracking (`onboarding.funnel.channel_first_message_latency`) across Telegram/WhatsApp/Slack/SMS, expanded funnel validators + redemption channel mapping, and added post-birth channel-connect DM prompt with <60s SLA target. Unblocked Convex bundling by splitting Node APIs into `convex/ai/tools/codeExecutionNodeRuntime.ts` (`\"use node\"`) and routing `codeExecutionTool` through internal node actions. Verify run exactly: `npm run typecheck` passed, `npm run lint` completed warnings-only (0 errors), `npm run test:unit` passed (130 files, 628 tests; 4 files/80 tests skipped). |
| `FOG2-011` | `E` | 5 | `P1` | `DONE` | `FOG2-007`, `FOG2-009` | Implement Day 0-3 nurture scheduler with first-win guarantee (<24h) | workflow scheduler + templates + per-user state | `V-TYPE`; `V-LINT`; `V-UNIT` | Done 2026-02-25: added Day 0-3 nurture lifecycle persistence (`onboardingNurtureJourneys`) and runtime orchestration (`convex/onboarding/nurtureScheduler.ts`), wired non-blocking journey starts from email signup (`convex/onboarding.ts`), OAuth signup (`convex/api/v1/oauthSignup.ts`), and identity claim consumption (`convex/onboarding/identityClaims.ts`), and added first-win breach auditing cron (`convex/crons.ts`). Verify run exactly: `npm run typecheck` passed, `npm run lint` completed warnings-only (0 errors), `npm run test:unit` passed. |
| `FOG2-012` | `E` | 5 | `P1` | `DONE` | `FOG2-011` | Ship Soul Report generation + Day 5 specialist preview timer contract | report generator + specialist access tables/runtime | `V-TYPE`; `V-LINT`; `V-UNIT` | Done 2026-02-25: shipped data-backed/channel-safe Soul Report runtime (`convex/onboarding/soulReportScheduler.ts`) with Day 5 specialist preview dispatch contract, added onboarding report/contract tables (`onboardingSoulReports`, `onboardingSpecialistPreviewContracts`) in `convex/schemas/webchatSchemas.ts` + `convex/schema.ts`, scheduled Day 3/Day 5 jobs from nurture start, and added helper coverage (`tests/unit/onboarding/soulReportScheduler.test.ts`). Verify run exactly: `npm run typecheck` passed, `npm run lint` completed warnings-only (0 errors), `npm run test:unit` passed (134 files, 652 tests; 4 files/80 tests skipped). |
| `FOG2-013` | `F` | 6 | `P0` | `DONE` | `FOG2-002`, `FOG2-004`, `FOG2-007`, `FOG2-009` | Add rollout flags/kill switches + migration path from current beta gate/manual approval flow | `convex/betaAccess.ts`; feature flag surfaces; docs | `V-TYPE`; `V-LINT`; `V-DOCS` | Done 2026-02-25: dependency precheck confirmed `FOG2-002`, `FOG2-004`, `FOG2-007`, and `FOG2-009` were `DONE`; added centralized rollout-policy/kill-switch controls in `convex/betaAccess.ts` (`resolveSignupBetaAccessDecision`, `setBetaOnboardingRolloutControls`, `setBetaOnboardingKillSwitch`); wired signup gating migration behavior in `convex/onboarding.ts`, `convex/api/v1/oauthSignup.ts`, and `convex/api/v1/emailAuth.ts`; updated feature-flag/admin surfaces in `src/components/window-content/super-admin-organizations-window/beta-access-tab.tsx`; published explicit kill-switch and rollback steps in `MASTER_PLAN.md` (`FOG2-013 Rollout + Rollback Runbook`) and synced `INDEX.md`, `MASTER_PLAN.md`, `TASK_QUEUE.md`, `SESSION_PROMPTS.md`. Top rollout risks reviewed before implementation: accidental auto-approve drift, channel bootstrap/latency regression, and partial-toggle rollback inconsistency. Verify run exactly: `npm run typecheck` passed; `npm run lint` passed with 3258 warnings and 0 errors; `npm run docs:guard` passed. |
| `FOG2-014` | `F` | 6 | `P1` | `DONE` | `FOG2-013` | Final verification + documentation closeout + launch checklist | tests + all workstream docs | `V-TYPE`; `V-LINT`; `V-UNIT`; `V-DOCS` | Done 2026-02-25 (closeout unblocked): prerequisite precheck confirmed all prior `P0` rows (`FOG2-001`, `FOG2-002`, `FOG2-004`, `FOG2-006`, `FOG2-007`, `FOG2-009`, `FOG2-013`) were `DONE`, and `FOG2-013` was `DONE` before execution. Pre-row top risks logged: rollout/kill-switch drift, rollback ambiguity under partial toggles, and channel bootstrap/latency regression. Resolved typecheck blocker by hardening optional action-context DB narrowing in `convex/ai/agentExecution.ts` (`resolveCrossOrgSoulReadOnlyEnrichment`) so `db.get`/`db.query` are safely callable after guard checks. Verify run exactly in order: `npm run typecheck` passed (exit `0`); `npm run lint` passed with `3261 problems (0 errors, 3261 warnings)`; `npm run test:unit` passed with `Test Files 136 passed | 4 skipped (140)` and `Tests 665 passed | 80 skipped (745)`; `npm run docs:guard` passed with `Docs guard passed.`. Kill-switch + rollback steps remain explicit and unchanged in `MASTER_PLAN.md` (`FOG2-013 Rollout + Rollback Runbook`, see `./MASTER_PLAN.md#fog2-013-rollout--rollback-runbook`) and `ROLLOUT_CHECKLIST.md`; rollback remains policy-only with no Telegram/WhatsApp/Slack/SMS handler or first-message latency tracking changes. Files changed: `convex/ai/agentExecution.ts`, `docs/reference_docs/topic_collections/implementation/free-onboarding-global/INDEX.md`, `docs/reference_docs/topic_collections/implementation/free-onboarding-global/MASTER_PLAN.md`, `docs/reference_docs/topic_collections/implementation/free-onboarding-global/TASK_QUEUE.md`, `docs/reference_docs/topic_collections/implementation/free-onboarding-global/SESSION_PROMPTS.md`, `docs/reference_docs/topic_collections/implementation/free-onboarding-global/ROLLOUT_CHECKLIST.md`. |
| `FOG2-015` | `G` | 7 | `P0` | `DONE` | `FOG2-014`, `AGP-023@READY` | Converge onboarding-v2 birthing to clone-first catalog activation: remove onboarding-exposed operator free-form create defaults, route birthing through protected-template clone lifecycle, auto-assign `isPrimary=true` for the first successful clone when no primary exists in `orgId + userId`, show capability-limit snapshot (`available now` vs `blocked`) in onboarding handoff, and route no-fit outcomes to purchase-only custom-agent concierge | `src/components/window-content/ai-chat-window/index.tsx`; `src/contexts/builder-context.tsx`; `convex/ai/agentExecution.ts`; `convex/ai/workerPool.ts`; `convex/agentOntology.ts`; onboarding docs copy | `V-TYPE`; `V-LINT`; `V-UNIT`; `V-DOCS` | Done 2026-02-25: onboarding creation kickoff now enforces catalog-clone-only handoff contract with explicit capability snapshot + concierge term copy, builder setup-mode bootstrap now fail-closes behind clone-first guard (`builder_clone_first_enforced`), `spawn_use_case_agent` now supports catalog preflight payloads (`available now` vs `blocked`) and blocked no-fit escalation metadata, and worker-pool clone lifecycle now auto-assigns `isPrimary=true` for the first successful owner-context clone (new or reused) when no primary exists in `orgId + userId`. Preserved `FOG2-013` rollout/kill-switch behavior (policy-only; no channel-handler rewrites). Verify run exactly in order: `npm run typecheck` passed; `npm run lint` passed with `3273 problems (0 errors, 3273 warnings)`; `npm run test:unit` passed with `Test Files 139 passed | 4 skipped (143)` and `Tests 674 passed | 80 skipped (754)`; `npm run docs:guard` passed (`Docs guard passed.`). |
| `FOG2-016` | `G` | 7 | `P1` | `DONE` | `FOG2-015` | Add regression coverage + operator runbook updates for onboarding clone-first birthing (`isPrimary=true` first-successful-clone behavior in `orgId + userId`, capability-limit transparency, one-visible-operator copy contracts, subtext/tone personalization capture, and fallback messaging when required integrations are missing or when no-fit routes to purchase-only custom-agent concierge) | `tests/unit/onboarding/*`; `docs/reference_docs/topic_collections/implementation/free-onboarding-global/MASTER_PLAN.md`; `docs/reference_docs/topic_collections/implementation/free-onboarding-global/ROLLOUT_CHECKLIST.md` | `V-TYPE`; `V-LINT`; `V-UNIT`; `V-DOCS` | Done 2026-02-27: added lane-G regression suite `tests/unit/onboarding/cloneFirstBirthingRegression.test.ts` covering first-successful-clone primary assignment guardrails (`hasPrimaryForOperator`), one-visible-operator kickoff copy and capability-limit transparency contracts, onboarding tone/communication personalization capture (`ONBOARDING_INTERVIEW_TEMPLATE`), and fallback messaging contracts (`CATALOG_CLONE_CAPABILITY_LIMITS_BLOCKED_MESSAGE`, `NO_FIT_CONCIERGE_TERMS`). Refactored kickoff contract into pure module `src/components/window-content/ai-chat-window/onboarding-kickoff-contract.ts` (behavior preserved) and exposed deterministic contract helpers/constants in `convex/ai/workerPool.ts`, `convex/ai/agentExecution.ts`, and `convex/ai/agentStoreCatalog.ts` for regression assertions. Preserved `FOG2-013` rollout/kill-switch/rollback semantics (policy-only; no channel-handler rewrites). Verify run exactly in order: `npm run typecheck` passed; `npm run lint` passed with `3377 problems (0 errors, 3377 warnings)`; `npm run test:unit` passed with `Test Files 164 passed | 4 skipped (168)` and `Tests 863 passed | 80 skipped (943)`; `npm run docs:guard` passed (`Docs guard passed.`). |

---

## Current kickoff

- Active task: none.
- Next promotable task: none (all queued rows `FOG2-001`..`FOG2-016` are `DONE`).
- Objective: keep `FOG2-013` rollout/kill-switch/rollback policy boundaries stable during subsequent unrelated workstreams.
