# ORV-012 Hardware Network Impairment Automation

## Goal

Close the remaining ORV-012 gap by executing repeatable radio/link-layer impairment tests on physical mobile hardware and capturing deterministic artifacts.

## Runner

- Script: `/Users/foundbrand_001/Development/vc83-com/scripts/mobile/voice-radio-chaos-hardware.mjs`
- NPM shortcuts:
  - `npm run test:e2e:mobile:hardware:chaos:plan`
  - `npm run test:e2e:mobile:hardware:chaos`

## Android physical-device execution

1. Connect device with USB debugging enabled.
2. Confirm device:
   - `adb devices`
3. Run plan-only preview:
   - `node scripts/mobile/voice-radio-chaos-hardware.mjs --platform android`
4. Run impairment execution:
   - `node scripts/mobile/voice-radio-chaos-hardware.mjs --platform android --device-id <ADB_DEVICE_ID> --run`

Automated scenarios:

1. `radio_wifi_drop_12s`
2. `radio_cellular_drop_12s`
3. `radio_toggle_burst_3x`

Captured artifacts per scenario include:

1. `*.connectivity.txt` (`dumpsys connectivity`)
2. `*.logcat.txt` (`logcat -d -v threadtime`)
3. `timeline.ndjson`
4. `manifest.json`
5. `summary.json`

## iOS physical-device execution

iOS radio controls are not safely scriptable through public CLI APIs on physical devices.
Run:

- `node scripts/mobile/voice-radio-chaos-hardware.mjs --platform ios`

This generates deterministic manual scenario definitions and telemetry expectations in:

- `ios-manual-scenarios.json`

Manual scenarios:

1. `ios_manual_airplane_burst`
2. `ios_manual_wifi_only_drop`
3. `ios_manual_cellular_drop`

## Required evidence mapping

For each scenario, include:

1. Runtime artifact bundle generated by the script.
2. Backend/mobile telemetry proving:
   - `fallback_transition`
   - `reconnect`
   - correlation invariant continuity (`liveSessionId + voiceSessionId`)
3. Any provider timeout/failure event taxonomy evidence for degraded links.
